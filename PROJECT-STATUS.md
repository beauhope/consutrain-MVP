# حالة مشروع ConsuTrain

## النسخة الفرنسية لمحاكي إدارة المشاريع — FR-1 (2026-08-14)

- بدأت النسخة الفرنسية في `fr/tools/project-exam-lab/` كأداة عامة مملوكة لـ ConsuTrain وموجهة لجميع الزوار، ولا ترتبط بأي مستخدم فردي.
- تشمل FR-1 الواجهة الفرنسية وبنية التطبيق وPWA والتخزين المستقل فقط.
- بدأ `fr/tools/project-exam-lab/data/questions.json` في FR-1 كنسخة تقنية مؤقتة مطابقة للبنك العربي؛ اكتملت ترجمته عبر FR-2 إلى FR-6، ولا تعد النسخة جاهزة للنشر العام قبل FR-7.
- صفحات النسخة الفرنسية تحمل مؤقتًا `noindex,nofollow`، ولم تُربط بطاقة `fr/tools.html` بها.

### FR-2 مكتملة (2026-08-14)

- تُرجمت الأسئلة Q001–Q100 ترجمة فرنسية مهنية، وشملت النصوص والخيارات والتفسيرات ودراسة الحالة C1 وأسئلة الترتيب والمطابقة.
- اجتاز البنك فحوص العدد (500)، وتفرد `qid`، وثبات الحقول التقنية، وصحة `correct` و`correct_pairs` والترتيب؛ بقيت Q101–Q500 مطابقة للمصدر العربي.
- ما زالت النسخة `noindex,nofollow` وغير مرتبطة من `fr/tools.html` حتى FR-7.

### FR-3 مكتملة (2026-08-14)

- تُرجمت الأسئلة Q101–Q200 ودراسة الحالة C2، فأصبح النطاق Q001–Q200 متاحًا بالفرنسية.
- اجتازت الدفعة مراجعة لغوية ثانية وفحوص `correct` و`correct_pairs` والترتيب وثبات Q001–Q100 وQ201–Q500.
- تبقى Q201–Q500 بالعربية مؤقتًا، وتظل النسخة `noindex,nofollow` وغير مرتبطة من `fr/tools.html`.

### FR-4 مكتملة (2026-08-14)

- تُرجمت الأسئلة Q201–Q300 ودراسة الحالة C3، فأصبح النطاق Q001–Q300 متاحًا بالفرنسية.
- اجتازت الدفعة المراجعة الفرنسية الثانية وفحوص `correct` و`correct_pairs` والترتيب، مع ثبات Q001–Q200 وQ301–Q500.
- تبقى Q301–Q500 بالعربية مؤقتًا، وتظل النسخة `noindex,nofollow` وغير مرتبطة من `fr/tools.html`.

### FR-5 مكتملة (2026-08-14)

- تُرجمت الأسئلة Q301–Q400 ودراسة الحالة C4، فأصبح النطاق Q001–Q400 متاحًا بالفرنسية.
- اجتازت الدفعة مراجعة فرنسية مستقلة وفحوص `correct` و`correct_pairs` والترتيب، مع ثبات Q001–Q300 وQ401–Q500.
- تبقى Q401–Q500 بالعربية مؤقتًا، وتظل النسخة `noindex,nofollow` وغير مرتبطة من `fr/tools.html`.

### FR-6 مكتملة (2026-08-14)

- تُرجمت Q401–Q500 ودراسة الحالة C5؛ أصبحت الأسئلة Q001–Q500 ودراسات الحالة الخمس كاملة بالفرنسية.
- اجتاز البنك الكامل فحوص البنية والإجابات والترتيب والمطابقة والعربية المرئية دون أخطاء أو تحذيرات.
- تبقى النسخة غير منشورة رسميًا و`noindex,nofollow` وغير مرتبطة من `fr/tools.html` حتى مراجعة وتوحيد ونشر FR-7.

### FR-7 مكتملة (2026-08-14)

- أصبحت النسخة الفرنسية العامة متاحة في `/fr/tools/project-exam-lab/` و`https://consutrain.com/fr/tools/project-exam-lab/`، وتضم 500 سؤال فرنسي وخمس دراسات حالة.
- وُحدت المصطلحات، واكتملت مراجعة الأنواع والإجابات والتفسيرات، وأزيل الحجب عن صفحات الإنتاج مع بقاء صفحة 404 فقط `noindex`.
- أصبحت روابط AR/FR متبادلة، وربطت بطاقة `fr/tools.html` بالنسخة الفرنسية.
- تستخدم النسخة الفرنسية `projectExamLab_fr_` وCache الإنتاج `project-exam-lab-fr-v2-production` بنطاق مستقل.

آخر مراجعة موثقة: 2026-08-14

## محاكي إدارة المشاريع

- أضيف تطبيق عربي ثابت مستقل في `tools/project-exam-lab/`، ورابطه العام `https://consutrain.com/tools/project-exam-lab/`.
- بنك الأسئلة في `data/questions.json` هو المصدر الوحيد ويحتوي على 500 سؤال تدريبي محاكي، لا أسئلة اختبار فعلية.
- المحاولات والنتائج وتفضيلات العرض تحفظ محليًا في المتصفح ولا ترسل إلى خادم.
- Service Worker الخاص بالمحاكي مسجل بنطاق مجلده فقط، مستقلًا عن `sw.js` الرئيسي.

## الحالة العامة

ConsuTrain منصة ويب ثابتة ثنائية اللغة في مرحلة MVP متقدمة. المستودع يجمع صفحات التعلم والدورات والخدمات والموارد والأدوات، مع أجزاء مشتركة للواجهة، PWA، نماذج داخلية، تدريبات مجانية بشهادات رقمية، ودليل حي للمنصة يُنتج منه ملفا PDF آليًا.

هذه الوثيقة لقطة للحالة الحالية وليست سجلًا يوميًا. القرارات في `DECISIONS.md`، والقواعد في `PROJECT-RULES.md`، والأعمال المقبلة في `ROADMAP.md`.

## النشر والنطاق

- الاستضافة المعتمدة: GitHub Pages.
- النطاق الرسمي: `consutrain.com`، ومثبت في `CNAME`.
- إدارة DNS والطبقة الطرفية: Cloudflare.
- توثق السجلات السابقة استخدام Cloudflare Access لحماية مسارات الإدارة والواجهات الإدارية. لا تُعد هذه الوثيقة بديلًا عن اختبار الإعداد الخارجي الفعلي قبل أي تغيير أمني.

## العربية والفرنسية

- الموقع العربي هو البنية الأساسية في الجذر والمجلدات الموضوعية.
- توجد نسخة فرنسية واسعة تحت `fr/` تشمل الصفحات الرئيسية، التعلم، الخدمات، الدورات، الأدوات، الموارد، القوالب، النماذج، ودليل المنصة.
- التكافؤ بين اللغتين متقدم، لكنه لا يعني تطابق كل أصل قابل للتحميل؛ بعض الموارد الفرنسية لها ملفات DOCX فعلية، بينما لا تزال أصول أخرى عربية فقط.
- توجد بيانات مقالات عربية في `assets/data/articles.json` وبيانات فرنسية في `assets/data/fr-articles.json`. حجم المحتوى بين الملفين غير متكافئ حاليًا، لذا تبقى إدارة النشر الثنائي أولوية.

## الواجهة المشتركة وPWA

- الهيدر والفوتر العربيان موحدان عبر `partials/header.html` و`partials/footer.html`.
- النسخة الفرنسية تستخدم `partials/fr-header.html` و`partials/fr-footer.html`.
- التحميل المشترك والمسارات يعتمدان على `assets/js/includes.js` وقيم `data-root` في الصفحات.
- توجد ملفات PWA الأساسية: `manifest.webmanifest` و`sw.js` و`offline.html` و`assets/js/pwa.js`.
- توحيد الواجهة العربية والفرنسية مكتمل على الأسطح العامة الرئيسية، مع بقاء الفحص الدوري للمسارات والكاش ضروريًا عند كل توسع.

## النماذج وطلبات الأسعار والاستشارات

- توجد نماذج داخلية عربية وفرنسية لطلب الاستشارة في `services/consultation-form.html` و`fr/services/consultation-form.html`.
- توجد صفحتا طلب سعر منظمتان: `quote-request.html` و`fr/quote-request.html`.
- توجد صفحتا ملاحظات داخليتان: `feedback.html` و`fr/feedback.html`.
- النماذج المفحوصة ترسل إلى مسار Google Apps Script قائم. لا تُوثق عناوين الاستقبال أو أي بيانات حساسة هنا.
- يتوفر رابط واتساب سريع في الفوتر المشترك، إلى جانب النماذج المنظمة داخل الموقع.
- Tally ليس المسار المعتمد حاليًا ولا ينبغي إعادته إلى الصفحات أو الوثائق.

## الدورات والورش

- يوجد فهرس دورات عربي في `courses/index.html` وبنية دورات موضوعية تحت `courses/`.
- توجد واجهة دورات فرنسية في `fr/courses/` تشمل الفهرس وصفحات تعريف متعددة.
- توجد ورش تطبيقية قصيرة ضمن `courses/practical-workshops/`، ودورة اتصال داخلي مهنية متعددة الجلسات.
- البنية الحالية قابلة لإضافة دورات وورش جديدة، مع ضرورة الحفاظ على التكافؤ اللغوي حين يتوفر المحتوى باللغتين.

## التدريبات المجانية والشهادات الرقمية

- توجد أربعة تدريبات مجانية قصيرة بالعربية، وأربعة مسارات مقابلة بالفرنسية:
  - مقدمة في التحول الرقمي.
  - التحسين المستمر وكايزن.
  - أساسيات العلامة وتجربة العميل.
  - أساسيات استراتيجية المحيط الأزرق.
- تتضمن المسارات صفحات دروس وتقييمًا نهائيًا وطلب شهادة رقمية.
- تستخدم صفحات التدريب مسار إرسال موحدًا للشهادات، مع منع التكرار حسب التدريب والبريد وإظهار حالة التكرار للمستخدم.
- مصطلح الشهادة المعتمد هو شهادة إتمام رقمية، وليست اعتمادًا مهنيًا أو ترخيصًا رسميًا.

## أتمتة الشهادات وn8n

- اكتمل مسار إصدار الشهادة والتحقق منها واختُبر من البداية إلى النهاية على Production بتاريخ 5 سبتمبر 2026؛ الحالة **Production Ready / Verified End-to-End**.
- المرجع التشغيلي الكامل للـWorkflows وGoogle Sheets والقوالب والـAPI وصفحة التحقق والـQA هو [`CERTIFICATE-SYSTEM.md`](CERTIFICATE-SYSTEM.md).
- n8n هو طبقة الأتمتة المعتمدة، ويستخدم الإنتاج `ConsuTrain Certificate Queue Processor v2`؛ أما `ConsuTrain Certificate Queue Processor` القديم فهو Deprecated / Unpublished ولا يُعاد استخدامه.
- توجد لوحة داخلية في `admin/certificates/index.html` وواجهة إدارة عامة في `admin/index.html`.
- لوحة الشهادات في وضع قراءة فقط وقابلة للتوسع؛ لا يوجد في المستودع ما يثبت اكتمال إجراءات حساسة مثل إعادة الإرسال أو تعديل البيانات أو إدارة تخزين ملفات الشهادات إنتاجيًا من اللوحة.
- يجب عدم وضع روابط Webhook الداخلية أو بيانات الدخول أو الأسرار في وثائق المستودع؛ الاستثناء الموثق هو endpoint التحقق العام المنشور للزوار في `CERTIFICATE-SYSTEM.md`.
- لا توجد مشكلة تشغيلية مفتوحة في مسار الإصدار والتحقق العام؛ تبقى مراقبة n8n وتطوير أدوات الإدارة المحمية تحسينات مستقلة.

## دليل ConsuTrain الحي وملفات PDF

- الدليل العربي الحي: `consutrain-guide.html`.
- الدليل الفرنسي الحي: `fr/consutrain-guide.html`.
- مصدرا المحتوى: `assets/data/platform-guide.json` و`assets/data/fr-platform-guide.json`.
- بيانات الاتصال المشتركة: `assets/data/platform-contact.json`.
- يولد `scripts/generate_platform_guide_pdfs.py` ملفي PDF عربيًا وفرنسيًا بأسماء ثابتة داخل `resources/downloads/profile/`.
- يحتفظ `assets/data/platform-guide-pdf-manifest.json` ببصمات الإصدار ومسارات الملفات المولدة.
- يشغّل `.github/workflows/update-platform-guide-pdfs.yml` التوليد عند تغير مصادر الدليل أو المولد أو الأصول المحددة، ويتحقق من المخرجات ثم ينشئ commit آليًا عند وجود فرق.
- لذلك يجب تجنب تعديل ملفات PDF والـmanifest محليًا بالتوازي مع تشغيل GitHub Actions، ومراجعة نتائج العمل الآلي وسحب تحديثاته قبل بدء تعديل جديد.

## الموارد والأدوات والتعلم

- توجد مكتبة موارد مجانية في `resources/index.html` وقوالب مشروحة في `templates/`، مع ملفات تنزيل منظمة حسب المجال.
- توجد صفحات أدوات عربية تحت `tools/` وصفحات تعريف/توطين فرنسية تحت `fr/tools/`.
- توجد بوابة تعلم عربية وصفحات فرنسية مقابلة، ومسارات مهارات إدارية وتحليل استراتيجي ومكتبة معرفة.
- توجد بعض موارد Markdown القديمة بمسارات مسطحة إلى جانب النسخ المصنفة. لم تُحذف في هذه الدفعة وتحتاج مراجعة مستقلة قبل أي تنظيف لتجنب كسر روابط تنزيل قديمة.

## رادار أدوات الإدارة الرقمية والمقالات

- توجد صفحة رادار عربية في `learn/management-tech-radar.html` وصفحة فرنسية مقابلة في `fr/learn/management-tech-radar.html`.
- توجد تحليلات رادار عربية وفرنسية وصفحات لموضوعات الحوكمة والأتمتة وتكامل الذكاء الاصطناعي ومرونة النماذج وأنظمة العمل.
- تم تنفيذ التكافؤ المرئي والمحتوى الأساسي للرادار، لكن أتمتة المسار الكامل من التجميع إلى مسودة المقالة ثم المراجعة والنشر ليست مثبتة كمنظومة مكتملة داخل المستودع.

## أعمال مكتملة مؤخرًا

- أُغلق **Final Launch Gate** للمشروع بحالة **READY TO LAUNCH**. اجتازت Git وPrivacy Fix وTool Service Worker Fix والمسارات الرئيسية والشهادات وPWA جميعها حالة **PASS**، مع `P0 = 0` و`P1 = 0`. أصبح ConsuTrain MVP / PWA جاهزًا للإطلاق العام؛ وما تبقى من البنود المصنفة P2/P3 هو تحسينات لما بعد الإطلاق ولا يمنع الإطلاق.
- أُغلقت مرحلة **SEO / Structured Data** نهائيًا بحالة **COMPLETED / CLOSED**. شمل التدقيق النهائي 356 صفحة HTML عامة: تحتوي 296 صفحة على JSON-LD، مقابل 60 صفحة دون JSON-LD، بتغطية إجمالية 83.1%. تغطية صفحات Sitemap كاملة 296/296 (100%)، وتغطية الصفحات القابلة للفهرسة، مع احتساب صفحة التحقق من الشهادة، هي 296/297 (99.7%). تحمل 59 من الصفحات الستين غير المغطاة توجيه `noindex`؛ والاستثناء الوحيد القابل للفهرسة هو `certificate-verification.html`، وإضافة `WebPage` إليها تحسين اختياري غير مطلوب.
- اجتازت الصفحات المغطاة QA نهائيًا: JSON-LD صالح 296/296، وcanonical موجود ومطابق لرابط الصفحة 296/296، دون كتل JSON-LD مكررة أو أنواع Schema متعارضة أو قيم فارغة أو `null` أو placeholders غير مستبدلة أو روابط كيانات غير صالحة أو اختلافات `http`/`www` أو تعارضات لغة فعلية أو hreflang غير صالح/مكرر. الكيانان العالميان المعتمدان هما Organization بالمعرّف `https://consutrain.com/#organization` وWebSite بالمعرّف `https://consutrain.com/#website`.
- خريطة الإغلاق النهائية: Core وServices وLearn Collections وArticles وTraining وTemplates مكتملة، وTools مكتملة للصفحات المنشورة، ولا توجد فجوات مطلوبة في Forms أو Legal أو Other؛ أما Verification فلا يتبقى فيها سوى تحسين `WebPage` اختياري. نُفذت التغطية عبر commits `e5cae68` و`8225f04` و`beff93e` و`bb9ed97` و`d612ec7` و`1a29858` و`98b33bb` و`de5734f` و`ac3a990` و`dab6031`، مع محاذاة أسماء البيانات المنظمة الأساسية في `bb3a629`.
- أُغلق Batch `4C-3B-1 Article Structured Data Enrichment` بحالة **COMPLETED / CLOSED** عبر commit `dab6031` (`SEO: enrich article structured data`). شمل 62 صفحة مقال فردي، وحقق QA تغطية `Article` و`image` و`author` و`isPartOf` وصلاحية JSON-LD بنسبة 62/62. تطابق `image` في جميع الصفحات مع `og:image`، ويستخدم `author` كيان `Organization` باسم `ConsuTrain` ومعرّف `https://consutrain.com/#organization`، ويرتبط `isPartOf` بـ`https://consutrain.com/#website`. بقيت قيم canonical و`mainEntityOfPage` و`headline` و`description` و`inLanguage` و`publisher` و`BreadcrumbList` دون تغيير، ولم تُعدّل ملفات HTML خارج نطاق الصفحات الـ62. لم تُضف `datePublished` أو `dateModified` لعدم توفر تواريخ نشر أو تعديل موثوقة. كانت بيانات `Article` الأساسية موجودة مسبقًا منذ commit `beff93e04b949d4b4242be737fbc6156287fba27` (`SEO: add structured data for articles and knowledge pages`)، ولذلك كانت 4C-3B-1 دفعة إثراء وإنهاء وليست إعادة بناء.
- أُغلق Batch `4C-3A Learn / Collections` بحالة **COMPLETED / CLOSED** بعد جرد 30 صفحة: 17 عربية و13 فرنسية. تستخدم جميعها `CollectionPage`، مع `BreadcrumbList` حيث يوجد breadcrumb فعلي، ولا تحتاج `ItemList` إضافيًا أو تكرار `Organization` و`WebSite`. تتطابق قيم canonical مع `url`، و`inLanguage` مع اللغة، و`name` مع H1، وتناسب `description` الصفحة، ويرتبط `isPartOf` بـ`https://consutrain.com/#website`. الاستثناء الصحيح هو `learn/ai-prompts.html`، إذ يستخدم `CollectionPage` فقط لعدم وجود breadcrumb فعلي في الواجهة. التنفيذ موجود أساسًا في commit `8225f04`، وفهرسا التدريبات المجانية في `d612ec7`؛ لا توجد صفحات تحتاج تعديلًا أو إعادة تنفيذ.
- أُغلق Batch `4C-2 Services` بحالة **COMPLETED / CLOSED**. أضاف commit `ac3a990e5f493dd5211c0ae62b35bfa844132ac0` (`final QA`) بيانات Structured Data إلى 26 ملفًا: صفحتا `CollectionPage` و24 صفحة `Service`. الـcommit موجود في `origin/main`، ولم تطرأ تغييرات لاحقة على هذه الصفحات، ولا حاجة إلى إعادة التنفيذ. سبب الالتباس السابق أن رسالة الـcommit لم تذكر SEO أو Structured Data.
- إطلاق الدليل الحي بالعربية والفرنسية.
- إضافة توليد PDF عربي وفرنسي آلي بأسماء روابط ثابتة وmanifest للإصدارات.
- إضافة workflow في GitHub Actions لتحديث أحدث ملفات PDF.
- توحيد الرادار العربي والفرنسي وإضافة مقال فرنسي لأنظمة العمل المدعومة بالذكاء الاصطناعي.
- توحيد تخطيط الفوتر العربي والفرنسي وتحسين تكافؤ طلب السعر.
- إبراز التدريبات المجانية بالشهادات في الصفحتين الرئيسيتين.
- تنظيف نصوص موجهة للمطور من واجهات الزائر وفق سجل Git الحديث.

## أعمال جارية

- تثبيت إدارة نشر المقالات العربية والفرنسية ومنع فجوة البيانات بينهما.
- استكمال أتمتة رادار التقنية من جمع الإشارات إلى مسودة قابلة للمراجعة.
- تحسين لوحة إدارة الشهادات وإجراءاتها المحمية بصورة آمنة وموثوقة؛ لا يشمل ذلك مسار الإصدار والتحقق العام المكتمل.
- تحسين مراقبة مسارات n8n والتنبيه إلى فشلها دون كشف تفاصيل تشغيل حساسة.
- متابعة جودة PWA والكاش والمسارات النسبية مع كل توسع.
- تنظيم الموارد القديمة المكررة بعد التحقق من الروابط المستخدمة فعليًا.

## الخطوات التالية ذات الأولوية

1. إكمال مسار رادار التقنية حتى المسودة والمراجعة البشرية.
2. تحسين نشر المقالات ثنائي اللغة وتوثيق قواعد إضافة المحتوى.
3. تطوير لوحة إدارة الشهادات وإجراءاتها الحساسة مع حماية مناسبة، دون اعتبار التحقق العام المكتمل عطلًا مفتوحًا.
4. إضافة مراقبة وتنبيهات لمسارات n8n الحرجة.
5. توسيع التدريبات المجانية والخدمات والموارد باللغتين دون الإخلال بالبنية القابلة للتوسع.
6. إجراء مراجعات دورية لـPWA وSEO والبيانات المنظمة والروابط والمسارات.
7. تحسين آلية تحديث الدليل وPDF بما يمنع تعارض التعديلات المحلية مع commits الأتمتة.

## قرارات سابقة محفوظة

شهد المشروع مراحل توسع متتابعة شملت مكتبة المعرفة، مسارات المهارات، الموارد والقوالب المجانية، الواجهة الفرنسية، النماذج، الشهادات، ولوحات الإدارة. حُذفت من هذه اللقطة تفاصيل التنفيذ اليومية والتكرارات فقط؛ أما القرارات المستمرة فأعيد توثيقها في `DECISIONS.md`، والأعمال المكتملة المهمة في `ROADMAP.md`.

## Tech Radar — Section Order

تم تعديل ترتيب صفحة رادار أدوات الإدارة الرقمية في النسختين العربية والفرنسية:

- `learn/management-tech-radar.html`
- `fr/learn/management-tech-radar.html`

أصبح قسم أحدث مقالات الرادار التقني يظهر مباشرة بعد الـ Hero، قبل أقسام موضوعات الرادار وطريقة الاستخدام، بهدف إبراز المحتوى المتجدد أولًا للزائر.

بقي تحميل المقالات الديناميكي من `articles.json` يعمل دون تعديل JavaScript أو CSS.

## بوابة ابدأ من هنا

تم تحسين صفحتي `start-here.html` و`fr/start-here.html` لتصبحا بوابة توجيهية مختصرة تشمل الدورات، الشهادات الرقمية، الورش التطبيقية القصيرة، المسارات التعليمية، الرادار التقني، الأدوات، القوالب والموارد، إضافة إلى طلب الاستشارة أو التدريب المخصص.
